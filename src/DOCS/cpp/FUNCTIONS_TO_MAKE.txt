add book(ID, title, author, location, category, status, if_checkid)
check(what_to_check, value)
add_category(value)
edit_category(value)
remove_category(value)
edit_book(previous_ID, ID, title, author, location, category, status, if_checkid)
delete(delete_what, ID)
search(search_what, search_value, search_by)
add_reader(name, surname, grade, class, ID, if_checkid)
edit_reader(previous_ID, ID, name, surname, grade, class)
add_loan(reader_ID, book_ID, time_return) -> makes ID and status itself
edit_loan(loan_to_edit_ID, reader_ID, book_ID)
unexpire_loan(ID, increment_return_date_by, status)
register(usr, pwd, role) -> makes hash itself, used in add_users too
login(usr, pwd) -> makes hash itself, then tries to find it in SQL DB
edit_user(usr,pwd,role) -> won't work while hashed
changerole_user(ID, role) -> won't work while hashed
add_database(name, type, encryption, private_key)
edit_database(name_previous, name, old_pkey, new_pkey, old_enc, new_enc)
backup_hdd(from, to, if_timestamped, if_log, if_compress)
backup_p2p(mode, port, if_whitelist, local_dir)
backup_server(mode, address, port, local_dir)
update_app(remove_old)
update_settings(background_color, if_sound)
update_networksettings(if_internet, if_p2p, if_remote, if_lan, if_lanp2p, if_lanserver)
load_current_databases(books, readers, loans)
save_current_databases(books, readers, loans, if_default)