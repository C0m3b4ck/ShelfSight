do that so that the program compiles, then make a checklist and see if the program is actually useable in terms of: logging in, adding books, readers, loans, managing loans
⚠️ Many buttons show "NOT IMPLEMENTED" - full functionality requires implementing the stubbed slots  
⚠️ Qt warnings about unmatched slots are expected (slots declared but no matching UI widgets)
